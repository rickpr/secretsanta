<%= bootstrap_form_for [@group, @rule], remote: true, layout: :inline do |f| %>
  <%= f.select :gifter, options_for_select(@group.santa.map &:name), hide_label: true %>
  <strong>cannot give to</strong>
  <%= f.select :recipient, options_for_select(@group.santa.map &:name), hide_label: true %>
  <%= f.submit class: 'btn btn-success' %>
<% end %>
