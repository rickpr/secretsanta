<% unless @rule.errors.any? %>
  var new_rule = $("<%= j render @rule %>").hide();
  $('#rules').append(new_rule);
  $('#rule_<%= @rule.id %>').fadeIn('slow');
<% else %>
  <% @rule.errors.full_messages.each do |message| %>
    $.growl.error({ message: "<%= message %>" });
  <% end %>
<% end %>
