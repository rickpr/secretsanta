<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>

<p>
  <strong>Owner:</strong>
  <%= @group.user.username %>
</p>

<h2>Santas</h2>
<div id="santa">
  <%= render @group.santa %>
</div>

<%= render 'santa/form' %>

<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
