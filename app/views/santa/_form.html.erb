<%= form_for([@group, Santum.new]) do |f| %>
  <% if @santum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@santum.errors.count, "error") %> prohibited this santum from being saved:</h2>

      <ul>
      <% @santum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
